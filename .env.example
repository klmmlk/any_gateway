# ============================================================
# any_gateway 环境变量示例
# 复制此文件为 .env 并填入实际值，切勿将 .env 提交到版本控制。
# ============================================================

# ---- LDAP / Active Directory 认证配置 ----

# LDAP 服务器地址（ldap:// 或 ldaps://）
# 示例: ldap://dc.company.internal
# LDAP_SERVER_URL=ldap://dc.company.internal

# LDAP 搜索基础 DN
# 示例: DC=company,DC=internal
LDAP_BASE_DN=DC=company,DC=internal

# NetBIOS 域名（用于拼接 DOMAIN\username 绑定字符串）
# 示例: COMPANY
LDAP_DOMAIN=COMPANY

# ---- 服务器配置 ----

# 网关服务端口
GATEWAY_PORT=8003

# ---- Admin API 密钥 ----

# 管理接口的认证密钥（Bearer token），用于保护 /admin/* 路由。
# 强烈建议使用高强度随机字符串，例如:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_KEY=your-admin-key-here    # 必须设置！用于保护 /admin/* 路由

# ---- 应急管理员登录 ----

# 当 LDAP 不可用时的离线 fallback 密钥。
# 使用方式: 用户名填 _admin_fallback，密码填此值。
# 强烈建议使用高强度随机字符串，例如:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_FALLBACK_KEY=your-fallback-key-here    # 可选，离线应急账号密码。运行: python -c "import secrets; print(secrets.token_urlsafe(32))"

# ---- React 前端 JWT 认证 ----

JWT_SECRET=your-jwt-secret-here
SUPERADMIN_USERNAME=superadmin


# ---- 其他配置 ----

# ---- 数据库 ----
# 不设置 = 本地 SQLite（./data/gateway.db）。
# 生产（腾讯云 TDSQL-C MySQL Serverless）示例:
# DATABASE_URL=mysql+aiomysql://root:password@gz-cynosdbpg.sql.tencentcdb.com:3306/gateway
# （PostgreSQL 也支持：postgresql+asyncpg://user:password@host:5432/gateway）
# DB_POOL_SIZE=5
# DB_MAX_OVERFLOW=10

# ---- 审计日志存储后端 ----
# local（默认，写 ./data/sessions）| cos（腾讯云 COS，serverless 多实例部署用）
# LOG_STORAGE_BACKEND=cos
# COS_REGION=ap-guangzhou
# COS_BUCKET=my-gateway-logs-1250000000
# COS_SECRET_ID=your-secret-id
# COS_SECRET_KEY=your-secret-key